# 시작하기 — 기본설정 UI/UX 개편 문서

이 저장소를 처음 받았다면 **이 페이지만** 보면 됩니다. (상세: [`README.md`](README.md) · [`docs/지침.md`](docs/지침.md))

## 1. 실행

```bash
npm install     # 최초 1회
npm run dev     # http://localhost:3000
```

| 열어볼 곳 | 무엇 |
| --- | --- |
| `/docs/overview` | 왜 바꾸는지 · 무엇까지 바꾸는지 (여기부터 보세요) |
| `/docs/basic-settings` | 개편 화면 + 영역별 설명. 상단 탭으로 카테고리 전환 |
| `/docs/pages` | 작업페이지 한눈에 보기(화면 목록 + 상태) |
| `/components` | 컴포넌트 표준(디자인 시스템) |

## 2. 문서 보는 법

화면 위 **분홍 번호**와 오른쪽 설명의 번호가 1:1로 이어집니다. 번호를 눌러 가며 읽으면 됩니다.

- 상단 **탭 칩**(전체 · 화면·노출 …)을 누르면 화면과 설명이 함께 그 탭으로 바뀝니다.
- 화면 안의 **「As-Is 대비 보기」**를 켜면 각 설정이 개편 전과 무엇이 달라졌는지 행 아래에 펼쳐집니다.
- **코멘트 달기**로 화면 아무 곳이나 찍어 의견을 남길 수 있습니다(브라우저에 저장).

## 3. 지금 확정이 필요한 것

문서에 「※ 확인 필요」로 표기돼 있습니다. 다섯 가지입니다.

1. 나머지 5개 탭의 항목 배분 (시안이 있는 탭은 「전체」·「화면·노출」 둘뿐)
2. 개편 화면에 없는 설정 7개의 처리 — 제거인지 이관인지 (문서 하단 표 「보류」)
3. 1:1 문의버튼을 어느 탭에 둘지
4. 하위 설정 계층을 무엇으로 보여 줄지 (`ㄴ` 표기 여부)
5. 나머지 영역의 토글도 함께 바꿀지

## 4. 기억할 3가지

1. **표준 컴포넌트로 조립** — `src/design-system`(배럴)에서 가져다 쓴다. 없으면 임시로 만들고 설명에 「디자인시스템 요청 대상」이라 남긴다. (직접 컴포넌트화 ✕)
2. **설명은 기능/예외 케이스로** — 컴포넌트명·색·px는 설명에 쓰지 않는다.
3. **작업은 브랜치 → PR** — `main` 직접 push 금지.

> AI 협업 규칙은 [`CLAUDE.md`](CLAUDE.md).
