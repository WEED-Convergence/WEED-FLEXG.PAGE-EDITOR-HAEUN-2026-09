# 시작하기 — 페이지 빌더 데이터 바인딩 문서

이 저장소를 처음 받았다면 **이 페이지만** 보면 됩니다. (상세: [`README.md`](README.md) · [`docs/지침.md`](docs/지침.md))

## 1. 실행

```bash
npm install     # 최초 1회
npm run dev     # http://localhost:3000
```

| 열어볼 곳 | 무엇 |
| --- | --- |
| `/docs/page-builder` | 에디터 화면 + 영역별 설명 (여기부터 보세요) |
| `/docs/pages` | 작업페이지 한눈에 보기(화면 목록 + 상태) |
| `/components` | 컴포넌트 표준(디자인 시스템) |

## 2. 문서 보는 법

화면 오른쪽에 영역별 설명이 번호와 함께 나옵니다. 번호를 눌러 가며 읽으면 됩니다.
**편집기라 요소가 촘촘해 화면 위에는 번호를 얹지 않았습니다.** 설명 쪽 번호만 보면 됩니다.

- 화면 안 **탭**(스타일, 애니메이션, 데이터)으로 오른쪽 패널을 바꿔 볼 수 있습니다. 이번에 설계한 곳은 **데이터** 탭입니다.
- 캔버스의 요소나 왼쪽 레이어 줄을 누르면 데이터 탭이 그 요소에 맞게 바뀝니다. 요소 목록에서 직접 골라도 결과는 같습니다.
- **코멘트 달기**로 화면 아무 곳이나 찍어 의견을 남길 수 있습니다(브라우저에 저장).

## 3. 시안에 없던 것은 이렇게 정했습니다

개선안 시안은 조건부 설정을 별도 탭으로 뒀는데, 어느 데이터에 건 조건인지 알기 어려워
**반복 데이터와 개별 데이터 각각의 아래**로 옮겼습니다.

고른 요소를 위에 한 번 더 보여 주던 카드는 같은 것이 두 번 나와 헷갈려서 빼고, 목록 안 체크로만 표시합니다.
그룹을 고르면 개별 데이터가, 낱개 요소를 고르면 반복 데이터가 잠기고 **왜 못 고르는지 칸 아래에 한 줄로** 나옵니다.

무엇을 왜 그렇게 정했는지, 개선안 대비 아직 안 된 것은 [`README.md`](README.md) 의 「정리한 판단」에 있습니다.

## 4. 기억할 3가지

1. **표준 컴포넌트로 조립** — `src/design-system`(배럴)에서 가져다 쓴다. 없으면 임시로 만들고 설명에 「디자인시스템 요청 대상」이라 남긴다. (직접 컴포넌트화 ✕)
   PAGE 서비스는 아직 표준 컴포넌트가 없어 이 화면은 전부 임시 조립입니다.
2. **설명은 기능/예외 케이스로** — 컴포넌트명·색·px는 설명에 쓰지 않는다.
3. **작업은 브랜치 → PR** — `main` 직접 push 금지.

> AI 협업 규칙은 [`CLAUDE.md`](CLAUDE.md).
